{
  "name": "fun-with-dev-js-common",
  "version": "0.1.0",
  "dependencies": {
    "npm": "^5.10.0"
  },
   "scripts": {
    "build": "npm run build",
    "publish": "npm run publish --access public"
  }
}
